{
  "openapi": "3.0.3",
  "info": {
    "title": "common-components"
  },
  "components": {
    "schemas": {
      "date": {
        "type": "string",
        "format": "date",
        "example": "1970-01-01",
        "x-chance": "date",
        "description": "A date in RFC3339 full-date format."
      },
      "dateTime": {
        "type": "string",
        "format": "date-time",
        "description": "A date in RFC3339 full-date full-time format."
      }
    }
  }
}
